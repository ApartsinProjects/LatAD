# Review 5: duplicate content, missing description, dense/jargon

Source: `poc/paper/IoT2.html` (revision2 branch), read line by line as it reads after accept-all. Line numbers are HTML source lines. Severity: H = fix before submission, M = should fix, L = polish.

---

## (a) DUPLICATE CONTENT

| # | Sev | Where | Issue (quoted) | Fix (keep / cut) |
|---|---|---|---|---|
| a1 | H | Fig 1 caption (l.157), Intro l.92, §7 "Implications for IIoT deployment" (l.1147-1155), Fig 1 in-image text | The deployment pipeline is narrated three times almost verbatim: "field sensors and PLCs feed an edge gateway or SCADA historian that aggregates and windows the multivariate telemetry, LatAD scores each completed window online, and an anomaly alarm together with the most-surprised subsystem is forwarded to the operator, maintenance, or security stack. Model fitting and calibration use train-normal telemetry only, so a site is commissioned from its own normal data without labeled attacks" (caption) vs "field sensors and programmable logic controllers emit telemetry that an edge gateway or SCADA historian aggregates into the same fixed windows used here; the trained encoder and per-community density models then score each completed window online, and an alarm together with the most-surprised community is forwarded to the operator, maintenance, or security stack. Two properties make this practical for IIoT: all model fitting and calibration use train-normal data only, so a site can be commissioned from its own normal telemetry without labeled attacks" (§7). | Keep the Fig 1 caption. Cut §7 Implications to two sentences: "Figure 1 is the deployment: the same windows, scored online, with no labels at commissioning. What §7 adds is the cost (Appendix B) and the subsystem shortlist (Table 7)." Delete the in-image sentence "Model fitting and calibration use train-normal telemetry only; no labeled attacks are required" from the SVG footer box or from the caption (one of the two). |
| a2 | H | Auto-gate rule: Intro l.185, §4 preamble l.445-447, §4.2 l.523-526, §4.3 iii l.582-586, l.594, note box l.596-597, Table 2 caption l.509, §7 l.1045-1051, §7 l.1084-1090, Conclusion l.1219 | "HAI and SWaT on, WADI off" and "fires where it generalizes to held-out normal" are stated ten times. The note box "Auto-gating summary. Base heads (i-ii) run on every dataset. The residual head (iii) fires when it generalizes to held-out normal (HAI and SWaT: on; WADI: off). All calibration uses train-normal only." is a pure restatement of the paragraph directly above it. | Keep §4.3 iii (rule + ratios 3.41/1.22/0.76). Delete the note box (l.596-597). §4.2 end: cut "(HAI and SWaT here, not WADI, where the residual separates the difficult subset but overfits the normal reference and is gated off)" to "(§4.3 iii)". §4 preamble l.445-447: cut "and activates on HAI and SWaT (Table 6)". §7 l.1050-1051: cut "reconstruction is added back only through the optional, auto-gated residual head where it demonstrably helps (§4.3 iii)" (the paragraph after Table 6 already says it). |
| a3 | H | Reachability-vs-probability: Intro l.88, l.162, l.166, l.184; §4.2 l.520-523; §7 l.1028-1032, l.1047-1049; Conclusion l.1217 | The Intro alone makes the point four times: "reconstruction asks whether a state can be reproduced, whereas detection needs to know whether normal operation is ever likely to occupy it" (l.88); "the question is not whether a flexible decoder can reproduce the state but whether normal operation is likely to occupy it. Reconstruction measures reachability; detection needs probability." (l.162); "because a flexible decoder reconstructs them faithfully" (l.166); "since a flexible decoder reconstructs the hard faults faithfully" (l.184). | Keep l.162 (crispest), §7 mechanism paragraph, and the Conclusion line. In l.88 cut "The reason is that reconstruction and normality answer different questions: ... occupy it." (leave the puzzle, resolve it at l.162). In l.166 cut ", because a flexible decoder reconstructs them faithfully". In l.184 cut "since a flexible decoder reconstructs the hard faults faithfully;". In §7 l.1047-1049 cut "Scoring by density in the clustered latent replaces reachability with probability, which is exactly what reconstruction cannot see." |
| a4 | H | Labels-are-scarce argument: l.90, l.92, l.160, §2.1 l.205, Fig 1 caption, §7 l.1151-1152, Conclusion l.1221 | "adopted because labeled anomalies are too scarce to train a classifier" (l.90); "Labeled faults are precisely what an IIoT operator lacks ... so learning the plant's normal law from normal data alone is the practical route" (l.92); "With a representative set of labeled faults the task would reduce to supervised classification; in practice genuine faults are rare ..." (l.160); "Because representative labeled faults and attacks are scarce and system-specific, the field leans on unsupervised ..." (l.205). | Keep l.90 (definition) and l.160 (the argument). Cut the l.92 sentence "Labeled faults are precisely what an IIoT operator lacks ... [6]" to a citation on l.90. In §2.1 reduce to "The field therefore leans on normal-only models [6,14], a setting LatAD shares." |
| a5 | H | §6 "Deep SOTA under raw metrics" (l.928-933) vs §6 "Difficult, joint-structure faults" (l.842-865) and Fig 3 caption (l.997) | Same numbers three times within one page: USAD 0.477 / TranAD 0.444 / LatAD 0.845 / WADI 0.579, 0.613 vs 0.771 / SWaT 0.658, 0.655 vs 0.837. Fig 3 caption repeats the HAI CI "[0.042, 0.157]" and SWaT "P = 0.007" from the prose above it. | Merge "Deep SOTA under raw metrics" into the difficult paragraph, keeping only its last sentence ("This is the concrete face of the illusion of progress ..."). Fig 3 caption: "Difficult-subset AUROC, values as Table 3; whiskers ±1 SD over five seeds where available. Significance tests are in the text." |
| a6 | H | WADI mechanism (0.634 -> 0.795 -> 0.771): §6 l.858-861, §7 l.1021-1024, §7 l.1059-1063, App A l.1242 | "the single-latent global density collapses to 0.634 on this difficult subset, below the linear baseline, so the community factorization rather than the single latent supplies the WADI signal" is given four times with the same three numbers. | Keep §6 (first statement) and App A (where Table A1 lives). In §7 "What the results show" cut the WADI clause after "P = 0.007)". In §7 "Why local density beats global density" replace the numbers with "(Table A1: 0.634 -> 0.795 on WADI)". |
| a7 | H | Table 2 caption (l.507-510) vs §4.2 prose (l.521-523) | Caption: "On HAI the reconstruction residual (0.689) is informative but stays below the latent term (0.760) and drags the summed joint score down to 0.689, so demoting reconstruction helps". Prose: "This is what happens on HAI, where the residual term (0.689) trails the latent term (0.760) and drags the summed joint score down, so demoting reconstruction helps." Verbatim. Table 2 itself (single model, WADI+HAI) is largely a subset of Table 6 (five seeds, three datasets). | Keep the prose. Caption becomes descriptive only: "Single trained model per dataset; the five-seed, three-dataset head ablation is Table 6." Better: add a "(a)+(b) joint NLL" row to Table 6 and delete Table 2 entirely, folding §4.2 into one paragraph that cites Table 6. |
| a8 | H | Table 6 narrated twice: §7 "Why reconstruction fails" (l.1037-1047) and the paragraph after Table 6 (l.1082-1090); Table 6 caption last sentence (l.1071) | "the single latent-density head ... exceeds ... USAD and TranAD on the difficult subset of all three plants (HAI 0.802 against 0.477 and 0.444; SWaT 0.794 against 0.658 and 0.655; WADI 0.656 against 0.579 and 0.613)" and "A reconstruction residual whitened on train-normal (Table 6) is a stronger term ... overtakes the density head on WADI and SWaT" (l.1045) then l.1082-1090 re-reads every cell: "On HAI reconstruction is informative but the weakest base head (0.695) while the latent density dominates (0.802); on WADI ... (0.740) ... (0.656) ... (0.634) ... HAI 0.801->0.820, SWaT 0.775->0.807 ... (0.835)". Caption adds "the deep reconstruction detectors of Table 3 score 0.44 to 0.67 on the same difficult subsets". | Keep the post-table paragraph as the single reading of Table 6. In "Why reconstruction fails" keep only the mechanism, the percentile analysis (median vs 93rd percentile, 27 percent), and one sentence "The latent-density head alone beats USAD and TranAD on every difficult subset (Table 6 vs Table 3)". Cut the caption's last sentence. |
| a9 | M | Double-hard numbers: prose l.892-898 and Table 4 caption l.909-913 | "+0.084 (95% CI [0.019, 0.189], P = 0.0005)", "+0.085 (95% CI [0.030, 0.146], P = 0.0005)", "+0.056, P = 0.43" all appear in both. The definition ("separated by neither the univariate max\|u\| rule nor the linear cross-channel predictor ... both thresholded on train-normal at the 99th percentile ... the detector under test playing no role") is also in both. | Caption keeps: definition in one clause, windows/episodes counts, seed policy. Prose keeps the numbers. Delete "The detector has the highest mean AUROC on all three datasets; the margin ... 7 episodes)." from the caption. |
| a10 | M | Table 5 caption (l.1004) vs "Where the gain comes from" (l.1000) | Both: "WADI's difficult anomalies are single-channel or linear, so per-channel marginals already suffice" and both list +0.200 / +0.141 / +0.017. | Caption: cut "The cross-channel gain is carried by HAI and SWaT; on WADI it is essentially zero, because ... suffice." |
| a11 | M | Localization rank rule: §4.4 l.655, §7 l.1153, §7 l.1159-1161, Table 7 caption l.1177 | "ordered by their train-normal upper-tail p-value (the quantity the Higher-Criticism combiner already uses)" (§4.4) = "The ranking is the one the Higher Criticism combiner already computes: communities are ordered by their train-normal upper-tail p-value" (§7) = "communities ordered by their train-normal upper-tail p-value" (caption). The 68-79 percent headline is in §4.4 and §7. | §4.4: "The per-community p-values also rank the subsystems, giving a triage shortlist (§7, Table 7)." Caption: "rank rule of §7". Numbers only in §7. |
| a12 | M | Benchmark scale: Intro l.176, §2.5 l.257, §7 "Benchmark scale and the trajectory assumptions" l.1132 | Intro: "SWaT (51 channels, 11 days), WADI (122 channels, 16 days), and HAI (59 channels, about 25 days) each span tens of channels, days to weeks, and a single nominal regime, whereas an operating plant ... orders of magnitude more streams over months to years [11] (§2.5)". §2.5 repeats the scale profile plus BDG2/N-CMAPSS. §7 repeats "with one nominal operating mode ... and days of data" plus BDG2/N-CMAPSS again, and §2.5 already closes with "flag operational scale as the setting in which the trajectory assumptions A9 and A10 and the masking form of A8 become testable (§7)". | Intro: replace the scale sentence with "These testbeds are also small and single-regime (§2.5)". §7: merge "Trajectory assumptions A9-A10" and "Benchmark scale and the trajectory assumptions" into one paragraph; name BDG2/N-CMAPSS only there, cut them from §2.5. |
| a13 | M | §2.2 l.222-223 and §2.5 l.257 | "which is why we read every method per dataset and per difficulty subset rather than by a single headline number" and "We therefore read every method per dataset and per difficulty subset". Also §2.5 "A parallel line of work argues that benchmark realism, not model novelty, now limits measured progress (Wu and Keogh; Sarfraz et al.; TSB-UAD)" re-cites §2.2's papers. | Cut both sentences from §2.5. |
| a14 | M | Intro l.174 vs contributions l.178-193; l.195-198 vs l.170-172 | l.174 "LatAD addresses the modeling obstacle: it replaces the reconstruction residual ... with a probability score in a jointly learned latent regime space, factorized over the plant's physical subsystems. The evaluation obstacle it addresses by adopting the critique's protocol" is contributions (2) and (3) in miniature. l.195-198 "Because aggregate benchmark scores are dominated by easy marginal excursions, the central test throughout is performance on the difficult subset" repeats l.170-172 "public CPS benchmarks are dominated by anomalies that a one-line univariate rule already separates ... Progress on the faults that matter cannot be measured until the easy majority is separated". | Delete l.174. Fold l.195-198 into contribution (3) as one clause ("the difficult subset, not the headline number, is the central test"); keep the seed/oracle disclosure sentence. |
| a15 | M | Seed policy (USAD/TranAD five-seed on HAI, single-run on WADI/SWaT): §2.4 l.244, §5.4 l.735, §5.5 l.759, Table 3 caption l.784, Table 4 caption l.906, Fig 3 caption l.997 | Six statements of the same policy. | Keep §5.5 (full) and Table 3 caption (short). Elsewhere "(seed policy: §5.5)". |
| a16 | M | GDN construct-matched description: §2.4 l.244-247, §5.5 l.762-765, Table 3 caption l.784 | "re-aggregating its per-timestep scores onto our window grid" three times; §2.4 also leaks a result ("on the difficult subset it collapses on HAI (0.481)"). | Keep §5.5. §2.4: "We also benchmark GDN under the same protocol (§5.5)". Remove the 0.481 from Related Work. |
| a17 | M | CPS definition: l.88 and l.90; IIoT expanded l.88 and §2.1 l.205 | Two consecutive paragraphs open "A cyber-physical system (CPS) couples networked sensors, actuators, and controllers with a physical process" and "A modern CPS (...) couples hundreds of such sensors, controllers, and actuators into a unit". §2.1 re-expands "Industrial Internet of Things". | l.90 open with "Such a system (a water-treatment plant, ...) has hundreds of these sensors ...". §2.1 use "IIoT". Also MIIM is expanded three times (abstract, l.179, l.271); keep abstract and §3, cut l.179's parenthetical. |
| a18 | M | §4 preamble l.443-450 vs Table 1 "Realized by" column and §7 | The paragraph re-lists which assumption maps to which head ("The mixture representation (A1, A6), the high-resolution density head (A2, A3), the low-dimensional latent (A4), the nearest-component score (A5) and per-feature standardization (A7) are always active ... A8 ... deferred ... A9-A10 ... future work (§7)"), all of which Table 1 already carries per row. | Cut to: "The reported detector realizes A1-A7 (Table 1, last column); A8-A10 are specified and measured but not realized (§7)." |
| a19 | M | Point-adjustment inflation: Intro l.168-172, §2.2 l.209-212, §5.5 l.766-768 | "point adjustment inflates F1 so severely that a random score outscores every deep model (§2)" is the third telling. | §5.5: "We do not apply point adjustment to any method (§2.2)." |
| a20 | M | Trivial rule described four times: §5.3 l.718-720, §5.5 l.768-770, §6 "Trivially separable anomalies" l.867-869, §6 "Validity of the difficulty split" l.871-876 | The two §6 paragraphs both say the trivial rule is strong on Easy by construction and tops SWaT easy (1.000). | Merge the two §6 paragraphs into one ("Validity of the difficulty split"). §5.5: "the trivial max\|u\| rule of §5.3 is also shown as a diagnostic row." |
| a21 | M | §5.3 l.721-724 pre-announces §6 l.878-886 | "§6 confirms the HAI advantage persists under a stronger split that also removes windows flagged by any of the six per-channel statistics, so difficulty is not defined by a single hand-picked statistic" then §6 says it in full. | §5.3: "(a stronger split is tested in §6)". |
| a22 | M | Edge cost: §7 l.1191-1194, App B prose l.2853-2859, Table B1 caption l.2864-2865 | "trains roughly 47 to 72 times faster than TranAD ... within about 3.5 to 3.9 GB of host memory" three times. Within the §7 paragraph, CalexNet's applicability is stated twice: "which applies directly to the per-community expert stack on constrained edge hardware" (l.1190) and "The CalexNet early-exit path above is a further-reduction route on top of these figures for the per-community stack on constrained edge hardware" (l.1193-1194). | §7: one sentence "Training and inference cost are in Appendix B (Table B1); early-exit cascades [57] are a further reduction route." Caption: drop the two interpretive sentences. |
| a23 | M | §7 "What the results show" l.1015-1026 | Restates §6 with every number (0.845, [0.042, 0.157], 0.44-0.48, 0.837 vs 0.782, P = 0.007, 0.795, 0.634, 0.771) then ends with the Conclusion's sentence "Modeling normal as a jointly learned latent plus explicit regime clustering, scored by probability and factorized over the plant's subsystems, is what delivers the advantage." | Cut to three sentences without numbers; keep the mechanism claim. |
| a24 | L | §3 l.339-342 | Two consecutive "pays off" sentences: "ordering tracks where the design pays off: the regime-community factorization gains most on WADI ..." then "This is the regime in which a jointly learned latent, scored by a mixture density and factorized over subsystems, pays off and a global detector or a raw reconstruction residual does not." | Delete the second. |
| a25 | L | §5.2 l.696 and l.712; l.675 and l.682 | "windows are length 60 with stride 30" twice in one subsection; "with the train and test horizons matched" in both WADI and SWaT bullets. | Cut from "Model configuration"; move "train and test horizons matched" into the l.689 downsampling paragraph once. |
| a26 | L | §6 l.1000 vs §5.1 l.671-674 vs Limitations l.1198 | The dropped WADI channel is explained three times, and §6 gives a different reason ("inflates single-channel separability") from §5.1 ("rescaled between the normal and attack recordings"). | Cut the §6 clause; keep §5.1 and the Limitations pointer. |
| a27 | L | Design-axes sentence: §4.4 l.660-662, §7 l.1134-1136, App A l.1238-1239 | "one point on each of four design axes: representation (VaDE), density estimation (itself two decisions ...), scoring heads, and calibration" appears in §4.4 and App A verbatim. | Keep App A; §4.4: "Appendix A ablates the density-estimation axis." |
| a28 | L | Table 3 prose l.775-777 vs caption l.782-786 | "Bold marks the best AUROC within a column ...; AUROC for the learned detectors is reported as mean±standard deviation over five seeds" is in both. | Cut from the prose. |
| a29 | L | Local-fault dilution: Intro l.188, §4.4 l.613-617, §7 l.1053-1058 | "a single global density dilutes such a local violation across the full dimensionality ... whereas a per-community density concentrates it" and §7 "its surprise is averaged down by the many communities that stay normal ... Estimating density inside each community removes that dilution". | Keep §4.4. §7 "Why local density beats global density": one sentence of mechanism plus the Table A1 pointer. |

---

## (b) MISSING OR INSUFFICIENT DESCRIPTION

| # | Sev | Where | What is missing | Fix |
|---|---|---|---|---|
| b1 | H | Table 7 vs §7 l.1171-1173 | Numbers disagree: Table 7 top-1 is HAI 0.39 (15/38) and WADI 0.33 (3/9), but the size-weighted comparison in prose reads "HAI's top-1 margin is small (0.42 versus 0.37, WADI 0.43 versus 0.19)". 0.43 = 3/7 (coverable episodes only) and 0.42 ≈ 15/36, so the prose uses a different denominator than the table without saying so. | State the denominator ("over the 7/36 coverable episodes") or recompute on one denominator; add "coverable episodes" as a column for all three datasets (only WADI has "(7 coverable)"). Also state the test behind "P = 0.004 and P = 0.02" (binomial against the coverage-aware rate?). |
| b2 | H | Community construction §4.4 l.608-611, §5.2 l.713-714 | "Agglomerative clustering on the train-normal correlation graph groups the channels into nested communities G (dendrogram subtrees of size 3 to 25 channels)" yielding "44, 28, and 25 communities". Not stated: the distance (1 − \|ρ\|? Pearson on raw channels or on window features?), the selection rule (every dendrogram node with 3-25 leaves? that would give far more than 44 on 122 channels), whether communities overlap in the reported runs, and the rule by which "regime count and latent dimension [are] scaled to the community size \|G\|". | Add to §5.2 Model configuration: distance metric, correlation source, the node-selection rule, the K(\|G\|) and latent-dim(\|G\|) formulas, and the resulting mean community size. |
| b3 | H | Reproducibility of the VaDE and heads §4.1 l.489-495, §5.2 | No architecture (layer widths, activations), epochs, batch size, optimizer, base and "slower" mixture learning rate, warm-up length, variance floor value, DAGMM penalty weight, pretraining epochs, density-head EM details. "capped at one tenth of the train-window count" (l.711) for M belongs in §4.3 i. Five seeds: what is seeded (network init, GMM init, Isolation Forest) and whether communities are recomputed per seed. | Add an Appendix D hyperparameter table (one row per setting, one column per dataset) and a one-line seed statement in §5.4. Move the M cap to §4.3 i. |
| b4 | H | Baseline configurations §5.5 | AutoEncoder (depth, bottleneck), Isolation Forest (trees, subsample), LinRes (regularization, ridge or OLS), USAD/TranAD (harness defaults? window length? epochs?), GDN (config, window), the HAI "downsampled rate" of the SOTA harness and the "integer-ratio correction" factor. | One paragraph or a table row per baseline with the exact settings; give the HAI harness rate (e.g. "1 score per 10 s, mapped 1:10"). |
| b5 | H | Dataset counts §5.1 / Table 3 headers | Only anomaly-window counts are given ("anom 56 = 26 easy + 30 difficult"). Missing per dataset: train-normal windows, test-normal windows (the AUROC negative class), number of attack episodes, attack fraction of timesteps (given for SWaT only), record lengths. Without train-window counts the reader cannot check the "one tenth" cap on M. | Add Table (Datasets): channels, train rows, test rows, attack share, episodes, downsampling, train/test windows, easy/difficult counts. Replace the "(anom ...)" header snippets with a pointer. |
| b6 | H | Eq (6) l.576-582 and Table 2 row (a) | Σ_k is called "precision" but written Σ_k^{-1} in Eq (6) (Σ is the covariance symbol); r is never defined as x − x̂ or which decoder produces x̂ (the VaDE decoder of the same expert?). r_k "reduced residual whitened by regime k's precision" then Eq (6) whitens again. | Define r = x − x̂ (VaDE decoder), P_k = shrunk precision of the PCA-reduced residual; write s_resid = Σ_k γ_k r̃ᵀ P_k r̃ with r̃ the 30-dim reduced residual. Same fix in Table 2 row (a). |
| b7 | M | Eq (4) l.541-547 | w_m, a_m, b_m never defined. | "with weights w_m, means a_m and diagonal standard deviations b_m fitted by EM on the train-normal latent". |
| b8 | M | Eq (7) l.590-594 | Whether s_resid is standardized like s_dens and s_near is not stated (l.741 implies yes). | s(x) = s_0 + g · std(s_resid), g ∈ {0,1} the gate. |
| b9 | M | §4.4 l.620-621, Eq (8)-(9) | How the per-community p-value is obtained (empirical train-normal CDF of s_G? of the community's s(x)?), what "calibrated surprise (the train-normal upper-tail negative log-probability)" is numerically (s_G = −log p_G?), and how the whole-plant expert enters (is it one of the S experts in HC or only summed afterwards? l.656-657 says summed). | One sentence: "p_G(x) is the empirical upper-tail probability of the community score under train-normal, s_G = −log p_G; HC runs over the S community experts only, and the whole-plant expert is added afterwards in standardized units." |
| b10 | M | Significance §5.4 l.745-752 | Block length "3 windows on WADI and 4 on HAI and canonical SWaT" is given without the rule that produces different lengths (window overlap is 2 windows on all three). Which baseline is "the strongest competing baseline" (by seed-mean difficult AUROC?) is not stated. "P ≈ 0" (l.847) should be a bound. | State the block-length rule; "strongest = highest seed-mean difficult AUROC in Table 3"; write "P < 0.0005 (0 of 2000 resamples)". |
| b11 | M | "canonical SWaT" | Used from §2.4 (l.244) onward, about twenty times, but "canonical" is only explained at §5.1 l.682 ("the canonical iTrust release (December 2015)"). Since the paper uses one SWaT configuration, the qualifier carries no information anywhere else. | Define once in §5.1, then write "SWaT" everywhere (Abstract included). |
| b12 | M | Abstract | "difficult subset" and "canonical SWaT" are undefined in the abstract; "VaDE" and "Higher Criticism" appear with no gloss. | "the difficult subset (anomalies a per-channel range rule cannot separate)"; drop "canonical"; "(VaDE, a variational autoencoder with a Gaussian-mixture prior)"; "combining per-subsystem surprises by a sparse-signal statistic (cohesion-weighted Higher Criticism)". |
| b13 | M | Table 5 l.1004-1010 | "cross-channel latent density" HAI = 0.789 but Table 6 "latent density" HAI = 0.802 (WADI and SWaT match to 0.001). Which run and configuration produced Table 5 is not stated. "channel-independent product of per-channel densities" is undefined (per-channel KDE on the six statistics? on a per-channel latent?). | Caption: name the run ("same five seeds as Table 6" or "independent run"), explain the HAI difference, and define the per-channel model. |
| b14 | M | §6 "Robustness to a stronger difficulty definition" l.878-886 | Reports only LatAD (global density) 0.675 against the baselines; the headline regime-community detector is absent; seed count not stated. | Add the regime-community value (or say why it was not run) and "five-seed mean". |
| b15 | M | §7 l.1040-1044 percentile analysis | "the reconstruction detectors place the difficult attacks at the median of normal operation, whereas the density head places them at the 93rd percentile; 27 percent of HAI's difficult attacks reconstruct at least as well as a typical normal window" — which reconstruction score (USAD, TranAD, or the whitened residual), which seed, what "typical" means. | "(TranAD score, seed-mean; 'typical' = median normal window)". |
| b16 | M | §7 "Benchmark coverage" l.1107-1115 | How the absent regime is identified (VaDE component? responsibility threshold?), what "flags this block as an unseen regime" computes (not in the scoring stack), and why the regime is "6.3% of test" while excluding it "removes 18.4% of test-normal windows". "true-positive rate at a 5% false-alarm budget" is a metric not introduced in §5.4. | Two sentences defining the regime-occupancy diagnostic and reconciling 6.3% vs 18.4% (test vs test-normal denominators, if that is the reason); introduce TPR@5%FA in §5.4 or drop it. |
| b17 | M | §7 Trajectory l.1118-1128 | A9 and A10 claims carry no table or appendix: "+0.06 difficult-subset AUROC on the part of the difficult subset whose history is attack-free" (n?), "three conditional scorers ... find no window ... above the shuffle floor" (numbers? how many windows screened?), "timescales span 3.4 to 3.7 decades" (how measured). | Either add Appendix D (A9/A10 screen: per dataset, n windows, scorer, floor, count above floor; the 600-sample result with n) or cut the paragraph to the qualitative claim with "(diagnostics in the released code)". Gloss "decades" as "log10 range of per-channel 1/e autocorrelation times". |
| b18 | M | §7 A8 paragraph l.1098 and Appendix C | Terms used without definition anywhere: "valley-separated neighbor within twice its own normal envelope", "lag-matched regime-switch base rate", "isolated anomalies", "discovered regime" (which model, K, which space), "min D" in Table C1 (Mahalanobis between what, in whose metric), "wide two-component head". Datasets Cranfield, SKAB, MetroPT, SMD, Paderborn have no channel/window counts, and Paderborn has no citation. | Appendix C: a definitions list (regime discovery model, envelope, valley, lag L, isolated anomaly, D) and a one-line table of the screened datasets (channels, windows, citation, including Paderborn). §7 keeps only the conclusion and the pointer. |
| b19 | M | Figure A1 caption l.2848 | "toy data with five Gaussian regimes ... three regime separations" but not: what "separation 4/6/10" is (centre spacing in std units?), how "fault position" is parameterized, what the reconstruction score is (PCA? an AE?), what the density score is, latent dim, number of faults per point, seeds. | Add two sentences of protocol to the caption or to the App A paragraph at l.1297. |
| b20 | M | §3 l.334-338 | "BIC-optimal K* of 22, 24, and 25 ... on a component grid extended to 25" (so K* sits at the grid ceiling on SWaT) yet the VaDE uses K = 20/40/40 (l.710); the relation between the BIC screen and the chosen K is not explained, and the space in which BIC and silhouette are computed (raw features, PCA, latent) is not stated. | State the space and add "K was set above K* to leave headroom (A2); the grid ceiling means K* is a lower bound". |
| b21 | L | Intro l.92 / Fig 1 | "PLC" is used in the Figure 1 image and caption ("field sensors and PLCs") but the acronym is never introduced (l.92 spells it out without the abbreviation). | l.92: "programmable logic controllers (PLCs)". |
| b22 | L | Intro l.187 | "cohesion-weighted" is used before cohesion is defined (§4.4 l.635). | Gloss: "(each subsystem weighted by how tightly its channels correlate)". |
| b23 | L | §4 preview l.348 | "surprise" is used from the abstract on but defined only at l.621. | Gloss at first §4 use: "surprise (negative log train-normal tail probability)". |
| b24 | L | §4.3 iii l.583 | Precisions are fitted on split A (80%) for the gate; whether the deployed precisions are refit on 100% of train-normal is not stated. | One clause. |
| b25 | L | Naming of the no-fusion configuration | The 0.795 configuration is called "community density aggregation (cohesion-weighted Higher Criticism over the community experts)" (§6), "community-density lead" (§7), and "LatAD (regime-community), cohesion-weighted HC, no whole-plant-expert fusion" (Table A1). The global expert is "global expert" (§4 l.347), "whole-plant expert" (§4.4), "unfactorized whole-plant expert" (l.622), "global detector" (l.622). | Pick one name each ("communities-only" and "whole-plant expert") and use it everywhere. |
| b26 | L | Table 2 l.505-517 | No SWaT column and no reason; "single trained model" seed not stated. | Add SWaT or a footnote; or delete the table (see a7). |
| b27 | L | Appendix B l.2853 | "Modal A10G" assumes the reader knows Modal is a cloud provider; training seconds for LatAD (regime-community) do not say whether community discovery and all experts are included; epochs not given. | "(Modal cloud, one A10G GPU)"; "wall-clock includes community discovery and all experts"; epochs in the hyperparameter table. |
| b28 | L | §5.2 l.697-698 | The six statistics include both min, max and range (= max − min); a reader will ask why. | One clause ("range is kept for the linear baseline's benefit" or drop it and say five). |

---

## (c) DENSE / OVERLOADED / JARGON (worst first)

| # | Sev | Where | Offending text | Split / gloss |
|---|---|---|---|---|
| c1 | H | §7 "Why reconstruction fails and density wins" l.1028-1051 | ~330 words, ten numbers, three cross-references, one sentence of 60 words: "Scoring by density rather than reconstruction is the right default: the single latent-density head, with no residual term and no factorization, exceeds the reconstruction-based deep detectors USAD and TranAD on the difficult subset of all three plants (HAI 0.802 against 0.477 and 0.444; SWaT 0.794 against 0.658 and 0.655; WADI 0.656 against 0.579 and 0.613) and is within noise of GDN on WADI (0.656 versus 0.660)." | Four short paragraphs: (1) mechanism (decoder interpolates between regimes); (2) evidence: "The latent-density head alone beats USAD and TranAD on every difficult subset (Table 6 vs Table 3)"; (3) the percentile analysis; (4) why the whitened residual is kept. Drop the inline number list. |
| c2 | H | Abstract l.82 | Sentence 2: "In cyber-physical systems normal is a union of many imbalanced operating regimes, formalized as ten MIIM assumptions, of which seven are realized by the detector and validated here and three are specified for future work; a fault can be individually normal on every sensor and reconstruct accurately, yet improbable under normal joint operation, so reconstruction-based detectors measure reachability rather than probability and miss it." Sentence 3 stacks "jointly learned latent Gaussian mixture (VaDE)", "correlation-community subsystems", "cohesion-weighted Higher Criticism", "auto-gated residual head". Results sentence has eight numbers and a P-value. | Split sentence 2 at the semicolon and move "of which seven ... future work" to the end of the abstract. Sentence 3 -> "LatAD models normal as a jointly learned latent Gaussian mixture and scores by probability, not reconstruction. It estimates that density per physical subsystem, found unsupervised from the correlation graph, and combines subsystem surprises with a sparse-signal statistic." Results: "best overall AUROC on HAI (0.948) and SWaT (0.938); on the difficult subset it leads all three, significantly on HAI (0.845) and SWaT (0.837), where USAD and TranAD fall to chance (0.44-0.48)." Drop the WADI P-value from the abstract. |
| c3 | H | §7 "Subsystem localization" l.1157-1173 | 260 words, ~20 numbers; limit (ii) is one 60-word sentence with three parentheticals: "against a size-weighted random baseline (a community drawn with probability proportional to its size) the SWaT top-1 margin holds (0.46 versus 0.35) while HAI's top-1 margin is small (0.42 versus 0.37, WADI 0.43 versus 0.19), which is why the three-community shortlist is the headline figure." | Put the size-weighted baseline as two extra columns in Table 7. Prose becomes five sentences: what is ranked, the top-3 headline, the top-1 result with P, the two limits in one sentence each. |
| c4 | H | §7 A8 paragraph l.1098 | Introduces "over-coverage", "masking", "precondition", "valley-separated neighbor within twice its own normal envelope", "30-percent-overlap positive control", "lag-matched regime-switch base rate", "wide two-component head" in one paragraph, most undefined. | Four sentences: what A8 posits; its two failure modes in plain words (a coarse density that fills the gap; an anomaly hidden by a neighbouring regime); "neither is observed on the studied benchmarks (Appendix C, Table C1)"; the over-coverage defence with the two percentages. Move every other number to Appendix C. |
| c5 | H | §4.4 l.618-624 | 90-word sentence with a nested parenthetical: "For each community G we therefore fit the full detector of §4.1-4.3 on that community's channels alone (a separate small VaDE per community, each with its own encoder, Gaussian mixture, and scoring heads, its regime count and latent dimension scaled to the community size \|G\|; nothing is shared across communities except the input window features, so the model is a stack of independent per-subsystem experts, tens per plant, plus one global expert). We read its calibrated surprise ..." | "For each community G we fit the full detector of §4.1-4.3 on its channels alone. Each community gets its own small VaDE, mixture, and heads, with K and latent dimension scaled to \|G\|; only the input features are shared. The result is tens of independent subsystem experts plus one whole-plant expert. Each expert emits a calibrated surprise s_G ..." |
| c6 | H | §6 WADI paragraph l.854-862 | One sentence lists five paired P-values, the next gives a CI and P, the next the mechanism with three more numbers. | Keep "significantly ahead of every learned detector (P ≤ 0.001 for each; per-detector values in the Table 3 caption or a footnote)" and the LinRes tie. Mechanism sentence stays but without repeating 0.795/0.634 if a6 is applied. |
| c7 | M | §6 Double-hard paragraph l.888-901 | Definition + three AUROCs + two CIs + two P-values + WADI numerical lead + trivial-rule drop in one paragraph. | Paragraph 1: definition (two sentences). Paragraph 2: "LatAD has the highest mean on all three (Table 4); the margin is significant on HAI and SWaT and numerical on WADI (7 episodes)." Numbers live in the Table 4 caption only (or the prose only; see a9). |
| c8 | M | §5.4 Significance testing l.745-752 | One five-line sentence: "Normal windows are resampled by a moving-block bootstrap with a block length of 3 windows ... so overlapping windows are resampled as blocks; anomalous windows are resampled by an episode-level bootstrap that draws whole attack episodes with replacement and contributes their difficult windows, preserving within-attack dependence." | Three sentences: what the paired statistic is; how normal windows are resampled (and why); how anomaly windows are resampled (and why); then what is reported. |
| c9 | M | Intro contribution (2) l.180-188 | A chain of four long sentences with "rare-regime-safe nearest-component likelihood", "mixture-density head", "auto-gated", "communities of the normal-data correlation graph", "cohesion-weighted Higher Criticism (HC)" all in a row. | Three sentences: what LatAD models (latent + regime mixture); how it scores (probability in the latent, reconstruction only as an optional gated head); how it factorizes (per subsystem, combined by a sparse-signal statistic). Keep the HC gloss. |
| c10 | M | Intro scope paragraph l.176 | Six sentences doing five jobs: faults vs attacks, slow faults, dataset scarcity, dataset scale, pointer to A8-A10. | Split into "Faults, not only attacks" (three sentences) and "What public data can show" (two sentences + pointer). The scale sentence goes to §2.5 (a12). |
| c11 | M | §3 l.331-342 | "cluster sizes are heavy-tailed, and Bayesian-information-criterion (BIC) regime counts keep improving past two dozen components (BIC-optimal K* of 22, 24, and 25 on WADI, HAI, and SWaT, on a component grid extended to 25). The regimes are least separated on WADI and HAI (silhouette 0.06 and 0.08, on a scale where 1 is well-separated clusters and 0 is adjacent, touching clusters) but separate more crisply on SWaT (0.29)." Parenthetical inside parenthetical. | "Regime counts keep improving past two dozen: BIC-optimal K* is 22, 24, and 25 on WADI, HAI, and SWaT (grid ceiling 25). Regime separation is low on WADI and HAI (silhouette 0.06 and 0.08) and higher on SWaT (0.29); silhouette 1 means well-separated clusters, 0 means touching clusters." |
| c12 | M | Table 1 caption l.281 and A8 row l.316-319 | Caption: "(A8's precondition present but its heavy overlap not exercised, A9 present in the data, A10 measured absent)". A8 row: "Its over-coverage variant is defended by the high-resolution density head; its masking variant is left with A10 as future work." These terms are defined only in §7. | Caption: cut the parenthetical (the group headers say it). A8 row "Motivates": "Not observed on the studied datasets; specified for future work (§7, Appendix C)". |
| c13 | M | §4.3 iii l.570-575 | One sentence: "reduces the per-channel residual r by principal-component analysis (fit on train-normal) to 30 dimensions, fits a Ledoit-Wolf-shrunk [33] (a covariance estimate regularized toward the identity, stable when channels outnumber samples) precision Σ_k per regime, and combines the per-regime whitened energies of that reduced residual by responsibility". | Three steps as three sentences (reduce; fit per-regime shrunk covariance, with the gloss as its own sentence; combine by responsibility). |
| c14 | M | §7 Trajectory l.1123-1128 | 70-word sentence: "A10 (path dependence) is measured absent rather than merely unobserved: three conditional scorers (a linear autoregressor on the two preceding windows, a nearest-neighbor successor model, and a regime-transition matrix), validated on a synthetic positive control and guarded by an order-shuffle control, find no window that is marginally normal yet improbable given its history above the shuffle floor, on all three benchmarks, once windows whose history already contains an attack are excluded." | "A10 is measured absent. We fit three history-conditioned scorers (a two-window linear autoregressor, a nearest-neighbour successor model, a regime-transition matrix), checked that each detects a synthetic path-dependent fault, and compared against an order-shuffled control. On all three benchmarks no window is normal on its own yet improbable given its history, once windows whose history already contains an attack are removed." |
| c15 | M | §6 "Where the gain comes from" l.1000 | One paragraph: representation vs density, Table 5 numbers, WADI channel exclusion, Table 6/A1 cross-reference. | Two paragraphs; drop the channel-exclusion clause (a26). |
| c16 | M | §7 "Benchmark coverage" l.1111-1114 | "Excluding that regime, a label-free operation that removes 18.4% of test-normal windows and no anomaly, lifts a secondary coverage-diagnostic view of the headline model (in that pass all-subset AUROC 0.948 to 0.965, difficult-subset 0.845 to 0.890, and the true-positive rate at a 5% false-alarm budget 0.789 to 0.916), with LatAD leading throughout." | "Excluding that regime removes 18.4% of test-normal windows and no anomaly. On the remaining test set the headline model rises to 0.965 all-subset and 0.890 difficult-subset AUROC (from 0.948 and 0.845). The main tables keep the full test set." |
| c17 | M | Appendix C l.2877-2894 | One 90-word sentence defines four named quantities inline: "the close-distinct pair mass (the share of ...), the nearest-pair overlap coefficient and density-valley ratio (heavy mode overlap itself), the labeled anomaly-masking rate beside its lag-matched regime-switch base rate (...), and the isolated-and-missed rate by head resolution (...)". | Four-item list, one definition each, then the Table C1 caption refers to the list instead of redefining. |
| c18 | M | Fig 2 caption l.440 | "each community, together with a whole-plant expert, is scored by a VaDE latent Gaussian-mixture model of normal regimes (A1/A6) through two always-on base latent heads (mixture density, A3; nearest-component NLL, A5) and one optional auto-gated whitened-residual head (A7)". | Split at "through": "... Gaussian-mixture model of normal regimes (A1/A6). Two base heads are always on (mixture density, A3; nearest-component NLL, A5); a whitened-residual head is optional and auto-gated (A7)." |
| c19 | L | §5.1 WADI bullet l.671-674 | "(its train-normal mean 9.09 and standard deviation 0.16 shift to a test-normal mean of 4503, a 27,359σ break, unique among the dataset's 123 raw channels)". | "(its mean jumps from 9.1 in the normal recording to 4503 in the attack recording; no other channel shows a rescaling)". |
| c20 | L | §5.5 l.755-759 | Baseline sentence with a colon, two appositives and an embedded definition; the USAD/TranAD sentence with the seed-policy parenthetical. | One sentence per baseline; seed policy as its own sentence. |
| c21 | L | §4.3 i heading l.534 | "Latent mixture-density head (parametric kernel-density estimate, KDE, for non-Gaussian pockets)": "parametric KDE" reads as a contradiction and "KDE" is never used again. | "Latent mixture-density head (a fine-grained Gaussian mixture used as a density estimator)". |
| c22 | L | §4 preview l.346-348 | Arrow chain of six undefined terms before §4.1. | Acceptable with Figure 2, but gloss "surprise" and "cohesion-weighted" at their first appearance here (b22, b23). |
| c23 | L | Table A1 caption l.1250-1256 | "Every aggregation-block row is fused with the whole-plant expert: the first four sum the community combiner and the whole-plant expert in train-normal standardized units (the headline fusion), while the last fuses unweighted Higher Criticism with the whole-plant expert by a maximum instead." | Move "fusion: sum" / "fusion: max" into the row labels and cut the sentence. |

---

### Suggested order of work
1. a1, a2, a3, a5, a7, a8 (cut the six largest repeats; roughly one page recovered).
2. b1 (Table 7 denominator), b2-b6 (reproducibility gaps).
3. c1-c6 (the six densest passages), then the rest of (a)-(c) in listed order.
